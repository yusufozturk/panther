query RuleDetails($input: GetRuleInput!) {
  rule(input: $input) {
    body
    dedupPeriodMinutes
    description
    displayName
    enabled
    id
    logTypes
    outputIds
    reference
    runbook
    severity
    tags
    tests {
      expectedResult
      name
      resource
    }
  }
}
